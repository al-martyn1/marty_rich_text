// Базовые стили, флаги

unknown,invalid=-1
clean,blank = 0

strong = 0x01 // выделение типа "важно", логический стиль, обычно это bold
emphasis   // выделение типа "некоторый акцент", логический стиль, обычно это italic
code       // код, логический стиль, обычно monospace шрифт
teletype   // клавиатурный ввод, логический стиль, обычно monospace шрифт
pre        // преформатированный текст


sub        // нижний индекс
sup        // верхний индекс

bold       // жирный - явный стиль, не логический
italic     // курсив - явный стиль, не логический
strikeout,strikethrough // зачеркнутый - явный стиль, не логический
underlined // подчеркнутый - явный стиль, не логический

#a
link       // гиперссылка

color      // цвет задан явно

font       // фонт задан явно

image      // картинка

style      // кастомный стиль

